/// <summary>
/// Enum to represent the different teams.  Used to set teams via the inspector.
/// </summary>
public enum EnumTeam {
    NONE = 0,
    ORANGE = 1,
    BLUE = 2,
    GREEN = 3,
    PURPLE = 4,
    BLACK = 99,
}
